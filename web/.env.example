# Copy to .env.local to override defaults.

# Base URL for the FastAPI platform API. Leave empty to use same-origin + MSW mocks.
VITE_API_BASE=

# Set to "true" to disable the MSW mock layer (e.g. when the real backend is up).
VITE_DISABLE_MOCKS=
